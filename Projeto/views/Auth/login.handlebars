<div class="auth-container"> {{! Classe de Formulario do nosso projeto, utilizado em outras views}}
    <h2>Entrar</h2>
    <form action="/login" method="post"> {{! Manda um Post para a url /Login}}
        <div class="form-control"> {{! Div utilizada para cada campo que vai ser digitado pelo cliente}}
            <label for="login">Login</label>
            <input type="text" name="login" placeholder="Digite seu login" required>
        </div>
        <div class="form-control">
            <label for="password">Senha:</label>
            <input type="password" name="password" placeholder="Digite sua senha" required>
        </div>
        <input type="submit" value="Entrar">
        <p>Não tem uma conta? <a href="/register">Clique aqui!</a></p>
    </form>
</div>